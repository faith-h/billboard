<h1>Click a Song To View It</h1>
<ul>
  <% @songs.each do |song| %>
    <li> <%= link_to song.name, [@artist, song] %></li>
  <% end %>
</ul>
<%= link_to 'Add Song', new_artist_song_path(@artist), class: "ui basic button" %>